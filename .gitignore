.idea/
.vscode/
/target

# build-android.sh 产物：cargo-ndk 输出的 .so + uniffi-bindgen 输出的 .kt
/mobile/android/app/src/main/jniLibs/
/mobile/uniffi-generated/
# Android Gradle 中间产物
/mobile/android/.gradle/
/mobile/android/build/
/mobile/android/app/build/
/mobile/android/local.properties

# 环境变量（含 secret，不入 git）
.env

# Claude Code 个人本地上下文
.claude.local.md

# cargo-mutants 输出
/mutants.out*/
/mutants-reverify/

# 本地 `cargo vendor` 输出，仅离线构建用
/pavao/
